import React from 'react';
import PropTypes from 'prop-types';
import './Notes.css';

const Notes = () => (
  <div className="Notes">
    Notes Component
  </div>
);

Notes.propTypes = {};

Notes.defaultProps = {};

export default Notes;
